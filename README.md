# StaticAnalysisTests
Code analysis samples / demo
Code can be compiled using standard cmake.

Two executables will be generated :
- one after fixing all warnings from cppcheck, clang tidy, gcc, clangarmv6.
- one before (I may have set a lot of bad code in it, at least 14 issues)
